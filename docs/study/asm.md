# CS1024M1 《汇编语言程序设计与调试》

## 用心投入，必有回响。

## 附录：复习参考方向自查

### 三种调用

| | near call | far call | int |
|:--|:--:|:--:|:--:|
| push 数量 | `______` | `______` | `______` |
| push 顺序 | `______` | `______` → `______` | `______` → `______` → `______` |
| 返回指令 | `______` | `______` | `______` |
| 返回时 pop | `______` | `______` → `______` | `______` → `______` → `______` |

### 堆栈框架
（near call + Pascal 方式）

```
SS:____  ______  ← BP 指向此处 ([bp+____])
SS:____  ______  ← 返回地址 ([bp+____])
SS:____  ______  ← [bp+____]
SS:____  ______  ← [bp+____]
```

### 中断向量与修改流程

1. 中断向量表位于 `______` ~ `______`（共 `______` 个 × `______` 字节）
2. int N 的向量地址 = `______`
3. 修改 int 8h 中断向量流程：`______` → `______` → `______` → `______` → `______`

### DOS 中断

**int 21h：**

| AH | 功能 | 入口 | 出口 |
|:--:|------|------|------|
| `______` | 键盘输入+回显 | — | `______` |
| `______` | 显示字符 | `______` | — |
| `______` | 显示 `$` 结尾串 | `______` | — |
| `______` | 退出 | `______` | — |

**int 00h：** `______`（触发条件：`______` 或 `______`，插在 `______` 指令的 `______`）

**int 01h：** `______`（触发条件：`______` = 1，用途：`______`）

**int 08h：** `______`（触发频率约 `______` 次/秒，用途：`______`）

**int 09h：** `______`（触发时机：`______`，读取端口 `______`）

### 标志位

| 标志 | 说明 | 相关指令 |
|:--:|------|------|
| CF | `______` | `______` |
| ZF | `______` | `______` |
| SF | `______` | `______` |
| OF | `______` | `______`; `jl` = `______` |
| IF | `______` | `______` |
| DF | `______` | `______` |

### 条件跳转

| 指令 | 条件 | 备注 |
|:--|:--|:--|
| `jl` | `______` | `______` |
| `jg` | `______` | |
| `jb`/`jc` | `______` | 等价 |
| `je`/`jz` | `______` | 等价 |
| `loop` | `______` | CX=0 时循环 `______` 次 |
| `repne` | `______` | 与 `loop` `______` |

### 字符串指令

| 指令 | 功能 | 寄存器 |
|:--|:--|:--|
| `repne scasb` | `______` | `______` |
| `rep movsb` | `______` | `______` |
| `lodsb` | `______` | `______` |
| `stosb` | `______` | `______` |
| `xlat` | `______` | `______` |

> 方向由 DF 控制：`cld`→`______`，`std`→`______`。

### 调试器

| 操作 | 按键/命令 |
|:--|:--|
| trace into（进入函数） | `______` |
| step over（跳过函数） | `______` |
| 硬件执行断点 | `______` |
| 硬件读断点 | `______` |
| 硬件写断点 | `______` |

### 间接寻址缺省段址

| 寻址方式 | 缺省段寄存器 |
|:--|:--:|
| `[bx]`, `[si]`, `[di]` | `______` |
| `[bp]` | `______` |
| `[bx+si]`, `[bx+di]` | `______` |
| `[bp+si]`, `[bp+di]` | `______` |

